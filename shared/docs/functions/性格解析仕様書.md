# 性格解析システム 仕様書

**ステータス**: 全フェーズ実装済み（週次スケジューラーはデプロイ要）
**作成日**: 2026-05-09
**最終更新**: 2026-08-15（性格キーの変化でもキャラクター詳細を再生成するヒステリシス付き判定を追加。夢の新候補をホーム画面のポップアップで通知。既存10ユーザーへの夢候補バックフィルを実施）

> 旧称「性格診断リニューアル設計書」。BIG5 100問方式を廃止し、チャットから継続的に性格を自動推定する新システムの完全仕様。

---

## 概要

チャットを続けるだけで自動的に性格タイプが解析されるシステム。「100問答える」作業は不要。アプリを使うほど自然に性格が浮かび上がる体験。

- 性格解析に必要なチャット数: **30回以上**（シグナル30件到達で元素・タイプ名が確定）
- 性格タイプ: 9元素 × 各2役割 = 18タイプ
- 旧BIG5診断（`confirmedBig5Scores`）は既存ユーザーの後方互換として残存するが、新規ユーザーへの提供は停止

---

## 5つの性格軸

すべてのタグが5軸すべての計算に使われる。元素・役割の判定はその結果から行う。

| 軸 | フィールド名 | -1.0（左端） | +1.0（右端） |
|----|------------|------------|------------|
| エネルギー | `energy` | 内向的 | 外向的 |
| 判断基準 | `judgment` | 感情重視 | 論理重視 |
| 関わり方 | `relationship` | 独立 | 協調 |
| 行動スタイル | `lifestyle` | 自由奔放 | 計画的 |
| 処理スタイル | `processing` | 直感 | 分析 |

---

## 軸スコアの計算方法

```
axis_score =
  (そのタグの+方向件数の重み合計) - (そのタグの-方向件数の重み合計)
  ─────────────────────────────────────────────────────────────
  (その軸に関連する全タグの重み合計)

※ 直近200件 or 直近90日のシグナルを対象
※ 時間減衰あり（decay = 0.95^経過日数）
→ 結果は必ず -1.0〜+1.0 に収まる
```

**更新タイミング**: `signalCount % 10 === 0` のとき（10シグナルごと）

---

## 9元素システム

### 元素の対応関係

外向と内向が鏡になる対称構造。対の元素は相性表の軸となる。

```
外向グループ               内向グループ
  炎（感情）       ↔         水（感情）
  風（判断中間）   ↔         土（判断中間）
  雷（論理×直感）  ↔         氷（論理×直感）
  光（論理×分析）  ↔         闇（論理×分析）

           無（エネルギー中間・真の中心）
```

### 各元素のイメージ

| 元素 | 性格イメージ |
|------|------------|
| 炎 | 情熱・衝動・感情が外に爆発する |
| 風 | 自由・好奇心・軽やか・感情にも論理にも縛られない |
| 雷 | 決断力・瞬発力・直感的な論理で外へ放電 |
| 光 | 明晰・計画的・分析的な論理で外を照らす |
| 水 | 共感・深さ・感情が内に深まる |
| 土 | どっしり安定・内に根を張る・ぶれない |
| 氷 | 冷静・冷たい本能・直感的な判断を内で凍てつかせる |
| 闇 | 内省・思索・分析的な論理を内に深める |
| 無 | 形がない・全てを受け入れる・観察者 |

### 元素カラー定義

| 元素 | カラー（HEX） |
|------|-------------|
| 炎 | `#E53935`（赤） |
| 水 | `#1E88E5`（青） |
| 風 | `#43A047`（ティール） |
| 土 | `#8D6E63`（ブラウン） |
| 氷 | `#4FC3F7`（ライトブルー） |
| 雷 | `#FDD835`（黄） |
| 光 | `#FFB300`（ゴールド） |
| 闇 | `#6A1B9A`（ダークパープル） |
| 無 | `#9E9E9E`（グレー） |

### 対の元素と相性

| 対 | 共通点 | 対立点 | 相性の性質 |
|----|--------|--------|----------|
| 炎 ↔ 水 | 感情重視 | 外に爆発 vs 内に深まる | 感情で深く繋がるが、ぶつかることも |
| 風 ↔ 土 | 判断が中間 | 自由に動く vs 動かない | 完全な対極、刺激し合うか相容れないか |
| 雷 ↔ 氷 | 論理×直感 | 熱く放電 vs 冷たく凍てつく | 同じ直感型、熱量の違いで惹かれ合う |
| 光 ↔ 闇 | 論理×分析 | 外を照らす vs 内を深める | 同じ分析型、視点の向きが逆 |
| 無 | ― | ― | 全元素と一定の相性（形がなく相手に合わせられる） |

---

## 元素判定ロジック

### Step 1：元素の決定（3軸使用）

```
エネルギー軸が中間（-0.3〜+0.3）
  → 無

エネルギー軸が外向（>+0.3）
  ├ 判断基準 < -0.3（感情重視）           → 炎
  ├ 判断基準 -0.3〜+0.3（中間）           → 風
  └ 判断基準 > +0.3（論理重視）
      ├ 処理スタイル < 0（直感寄り）       → 雷
      └ 処理スタイル >= 0（分析寄り）      → 光

エネルギー軸が内向（<-0.3）
  ├ 判断基準 < -0.3（感情重視）           → 水
  ├ 判断基準 -0.3〜+0.3（中間）           → 土
  └ 判断基準 > +0.3（論理重視）
      ├ 処理スタイル < 0（直感寄り）       → 氷
      └ 処理スタイル >= 0（分析寄り）      → 闇
```

### Step 2：役割の決定（元素ごとに使う軸が異なる）

| 元素 | 役割判定に使う軸 | 条件 | タイプ名 |
|------|--------------|------|---------|
| 炎 | 関わり方 | < 0（独立） | 独り燃える炎タイプ |
| 炎 | 関わり方 | >= 0（協調） | 場を沸かす炎タイプ |
| 風 | 行動スタイル | < 0（自由） | 自在に舞う風タイプ |
| 風 | 行動スタイル | >= 0（計画） | 目的へ吹く風タイプ |
| 雷 | 関わり方 | < 0（独立） | 単独で閃く雷タイプ |
| 雷 | 関わり方 | >= 0（協調） | 場を揺らす雷タイプ |
| 光 | 行動スタイル | >= 0（計画） | 道を照らす光タイプ |
| 光 | 行動スタイル | < 0（自由） | 先を駆ける光タイプ |
| 水 | 行動スタイル | < 0（自由） | 形を変える水タイプ |
| 水 | 行動スタイル | >= 0（計画） | 深く湛える水タイプ |
| 土 | 関わり方 | >= 0（協調） | 静かに支える土タイプ |
| 土 | 関わり方 | < 0（独立） | 独り立つ土タイプ |
| 氷 | 関わり方 | < 0（独立） | 研ぎ澄ます氷タイプ |
| 氷 | 関わり方 | >= 0（協調） | 凛として守る氷タイプ |
| 闇 | 行動スタイル | < 0（自由） | 深く問い続ける闇タイプ |
| 闇 | 行動スタイル | >= 0（計画） | 先を読む闇タイプ |
| 無 | 関わり方 | >= 0（協調） | 全てに溶け込む無タイプ |
| 無 | 関わり方 | < 0（独立） | 何にも染まらぬ無タイプ |

---

## タグ一覧と軸マッピング

| タグ | 検出する発言パターン | 影響する軸（方向） |
|------|------------------|----------------|
| social_reference | 誰かと会った、友人の話 | エネルギー(+) |
| solo_preference | 一人でやりたい、静かな時間 | エネルギー(-) |
| group_activity | みんなで、チームで | エネルギー(+) 関わり方(+) |
| initiating | 自分から声をかけた、提案した | エネルギー(+) |
| quiet_environment | 静かな場所、一人の時間が必要 | エネルギー(-) |
| planning_language | 計画を立てた、準備した、スケジュール | 行動(+) |
| spontaneous_language | なんとなく、流れで、気まぐれ | 行動(-) |
| goal_oriented | 目標、達成したい、結果を出す | 行動(+) |
| emotional_expression | 気持ちを吐露、感情豊かな表現 | 判断(-) 処理(-) |
| logical_reasoning | なぜなら、理由は、論理的に | 判断(+) 処理(+) |
| intuitive_decision | 直感で、ピンときた、なんか感じた | 判断(-) 処理(-) |
| data_driven | 調べたら、数字で見ると、データ | 判断(+) 処理(+) |
| cooperative_language | 一緒に、相手に合わせる、協力 | 関わり方(+) |
| independent_stance | 自分で決める、一人でやる | 関わり方(-) |
| self_paced | 自分のペースで、マイペースに、人に頼まず、合わせるのが苦手 | 関わり方(-) |
| change_seeking | 新しいこと、変えたい、試したい | 行動(-) 処理(-) |
| worry_anxiety | 心配、不安、どうしよう | 判断(-) |

---

## 更新・反映タイミング

| 処理 | タイミング | AIコール | 内容 |
|------|----------|---------|------|
| タグ収集 | メッセージごと | あり（classifyAndExtractに追加） | タグ抽出・Firestore保存 |
| 軸スコア計算 | 新タグ10件ごと（`classifyAndExtract.js` の `newCount % 10 === 0`） | なし（アルゴリズム） | 5軸スコア更新・タイプ判定 |
| キャラクター詳細生成 | signalCount 30到達時（初回）＋ **性格タイプが変化するたび** ＋ **性格キーがはっきり変わったとき** | あり（`gpt-4o-2024-11-20`）。ただし `CharacterDetailsTemplate/{personalityKey}` にヒットすればAIコールなし | 口癖・長所・趣味など10属性と、夢の候補5個を生成 |
| BIG5解析テキスト生成 | 同上（`generateCharacterDetails` 内で実行） | あり（`gpt-4o-2024-11-20`）。`Big5Analysis/{personalityKey}` にヒットすればAIコールなし | 適職・恋愛・ストレス・学習・意思決定の解析テキスト |
| ナラティブ生成 | 週次（毎週日曜2:00 JST） | あり（gpt-4o-mini） | 「最近〜な傾向があります」文生成 |
| 変化通知フラグ | 軸スコア計算時（タイプが変化した場合のみ） | なし | `pendingTypeChangeNotification` フラグをセット（FCM送信は将来実装） |
| 初回の夢選択フラグ | 初回のキャラクター詳細生成に成功したとき | なし | `pendingFirstDreamSelection` フラグをセット |

> **初回にタイプ変化通知は出ない**: 変化通知の条件は `typeChanged && prevElement !== undefined`（`axisCalculator.js`）。`element` は10シグナルごとに毎回計算・保存されるため、初めて `element` がセットされる回は `prevElement` が未定義で通知条件を満たさない。初回に性格が確定したことを伝えるのは `pendingFirstDreamSelection` を使った夢選択ダイアログの役割。
>
> **signalCount 100 では詳細は再生成されない**: 100到達は `growthStage` が 1→2（幼少期→大人）に変わるだけで、キャラクター詳細・夢の再生成は行われない。

### 性格キーの変化による再生成（ヒステリシス付き）

タイプ名も元素も変わらないまま Big5スコアだけが動くと、`personalityKey` と実際のスコアがずれて、口癖・長所・夢の候補が古い性格のまま残る。これを防ぐため、タイプ変化に加えて **性格キーがはっきり変わったとき** も再生成する（`axisCalculator.js`）。

判定は `const/generatePersonalityKey.js` の `hasDecisiveKeyChange()` が行う。

| 条件 | 再生成 |
|------|-------|
| 丸め値が変わり、丸め境界（x.5）から `KEY_CHANGE_MARGIN`（0.15）以上離れている | する |
| 丸め値は変わるが、境界から0.15未満（例 3.49 ↔ 3.60 の往復） | しない |
| 2段階以上動いた（例 3 → 1） | する |
| 性別が変わった | する |
| `personalityKey` が未設定・旧形式で解釈できない | する |

**ヒステリシスを入れる理由**: スコアは10シグナルごとに更新されるため、境界付近では 3.49 と 3.51 を往復するだけで丸め値が 3↔4 と変わり、そのたびに属性と夢の候補が作り直されてしまう。境界から一定以上離れて初めて変化と認める。

`parsePersonalityKey()` は接頭辞で解釈するため、並び順が違う旧形式（`O3_C3_A3_E3_N3`）や、丸めずに保存された小数キー（`O4.77…_C2.46…_男性`）も読める。

再生成時はフレンドとの相性診断も `isStale` にする（相性はBig5スコアから算出しているため）。

### 既存ユーザーへのバックフィル（2026-08-14 実施）

夢を5候補から選ぶ方式（2026-08-05）より前に性格が確定したユーザーは、`dream/current` が作られず候補を持たないため、詳細画面の「変更」ボタンが出ず夢を選び直せなかった。該当した10人に対して `generateCharacterDetails` を1回ずつ実行し、以下を是正した。

- 夢の候補5個を配布（既存の夢は上書きせず、`pendingDreamProposal` で提案）
- `personalityKey` を現在のスコアで再計算（小数キー5件・スコアとずれていた3件を是正）
- 口癖・長所などの属性を現在の性格で更新
- 新しいキーの `Big5Analysis` を8件生成（アプリはFirestoreを読むだけで自動生成しないため）

AIコールは キャラクター詳細10回 ＋ Big5解析8回 の計18回。以後は上記のキー変化判定により、同じ取りこぼしは起きない。

### 既存キャラの口癖バックフィル（2026-08-22 実施）

口癖に生成条件が無かった頃に作られた値が、相手への相づち（`「面白いね！」`）・単語だけ（`安定`）・鉤括弧込みで残っていた。`functions/scripts/backfillFavoriteWord.js` で **73キャラ / 60性格キー** の `favorite_word` だけを新条件で作り直した。

- 性格キーごとに1回だけAIを呼び、同じキーのキャラと `CharacterDetailsTemplate/{personalityKey}.attributes.favorite_word` に反映
- 既存の口癖を種として渡し、雰囲気を保ったまま独白でも使える形に直す（`「面白いね！」` → `これは面白い展開だ`）
- **口癖以外の属性と夢には触らない**。夢を更新しないため `pendingDreamProposal`（夢の再提案）は立たない
- 生成結果は12文字以内・鉤括弧なし・単語のみでないことをスクリプト側で検証し、満たさない場合は最大3回まで再生成する

---

## キャラクター画像との連携

5軸スコアをBIG5スコアに変換して既存の画像システムを継続使用する。新しい画像生成は不要。

```
Extraversion(E)      = (エネルギー軸 + 1.0) / 2.0 × 4.0 + 1.0
Conscientiousness(C) = (行動スタイル軸 + 1.0) / 2.0 × 4.0 + 1.0
Agreeableness(A)     = (関わり方軸 + 1.0) / 2.0 × 4.0 + 1.0
Openness(O)          = (-処理スタイル軸 + 1.0) / 2.0 × 4.0 + 1.0  ※反転
Neuroticism(N)       = (-判断基準軸 + 1.0) / 2.0 × 4.0 + 1.0      ※反転

結果はすべて 1.0〜5.0 に収まり、既存のL/M/H変換に渡せる
```

変換結果は `details/current.convertedBig5Scores` に保存。

**画像変更のタイミング:**

| signalCount | 性格解析実行 | 画像への反映 |
|------------|------------|------------|
| 10, 20 | ✅ 走る（element確定・Firestoreに保存） | ❌ 赤ちゃん固定（`signalCount < 30`） |
| 30 | ✅ 走る | ✅ 幼少期_{element} に初めて切り替わり |
| 40, 50 ... 90 | ✅ 10シグナルごとに走る | ✅ element変化があれば即反映 |
| 100 | ✅ 走る | ✅ 成人_{gender}_{element} に切り替わり |
| 110以降 | ✅ 10シグナルごとに走る（上限なし） | ✅ element変化があれば即反映 |

- 性格解析（`calculateAndSaveAxisScores`）は signalCount が10の倍数になるたびに実行（上限なし）
- 画像は `signalCount < 30 || element == null` の間は赤ちゃん固定
- 30以降はelement変化を検知したタイミングでFlutterがリアルタイムに画像を更新

---

## 変動の安定性

| メッセージ数 | 安定度 | 想定される変動 |
|------------|-------|-------------|
| 〜30件 | 低い | 元素・役割どちらも変動しやすい |
| 30〜100件 | 中程度 | 役割は変動、元素はほぼ安定 |
| 100件以上 | 高い | 元素は安定、役割はごく稀に変動 |

1回の更新（10件）で軸スコアが動く幅は最大0.05程度（200件ウィンドウの5%）。

---

## generateCharacterReply への5軸反映

`generateCharacterReply` はキャラクター返答生成時に性格特性テキストをプロンプトに渡す。  
`axisScores` が存在する場合は `buildPersonalityTraitsFromAxes()` で生成した5軸ベーステキストを優先使用し、存在しない場合は旧来の `buildPersonalityTraits(big5)` にフォールバックする。

```javascript
// templates.js
function buildPersonalityTraitsFromAxes(axisScores, element, typeName) {
  // 各軸の値からフレーズを選択・連結
  // 例: energy>0.3 → "外向的で人との交流から活力を得る"
  // 例: judgment<-0.3 → "感情や直感を重視して判断する"
  // typeName が存在する場合: "場を沸かす炎タイプ。外向的で..." の形で先頭に付与
}
```

### フェーズ別チャット返答との連携（2026-05-20 更新）

性格データの充実度（`signalCount`）に応じて、返答の深度（フェーズ）を切り替える。

| signalCount | 返答フェーズ | 性格反映 |
|------------|------------|---------|
| < 10（性格データ未確定） | Phase 1 固定 | **なし**（性格データ不十分のため誤言及を防ぐ） |
| 10〜29（初回解析済み・元素未確定） | Phase 2 固定 | **あり**（性格仮説を提示） |
| ≥ 30（元素確定済み） | session turn に応じて P1〜P3 | **あり** |

- **Phase 2**（signalCount 10〜29 の全ターン、または signalCount ≥ 30 の session turn 2〜4）: 性格特性を「仮説」として提示
- **Phase 3**（signalCount ≥ 30 かつ session turn 5〜）: 性格を言語化して説明。`typeName`（「炎タイプ」等）を自然に使う
- フェーズはFlutter側（`home_screen.dart`）で算出し、`phase` パラメータで Cloud Function に渡す
- `sessionChatCountProvider`（`chat_provider.dart`）でセッション内ターンを管理。アプリ起動時と性格リセット時に 0 にリセット

---

## 週次ナラティブ生成（Cloud Scheduler）

毎週日曜2:00 JSTに全アクティブユーザーの性格ナラティブを再生成する。

- **対象**: `element` フィールドが存在するユーザー
- **取得**: `collectionGroup('details').where('element', 'in', VALID_ELEMENTS)`
- **生成**: `「最近〜な傾向があります」`形式の100〜150文字テキスト（gpt-4o-mini）
- **保存先**: `details/current.personalityNarrative`・`narrativeUpdatedAt`
- **変化検知**: `element` または `typeName` が前回と異なる場合、`personalityMeta/current.pendingTypeChangeNotification = true` をセット

---

## Flutter UI

### キャラクター詳細画面

#### 性格タイプカード

**データ十分時（signalCount >= 30）:**

```
┌────────────────────────────────────┐
┃  [元素カラー左ボーダー 4px]         ┃
┃  場を沸かす炎タイプ                ┃  ← typeName（18px）
┃  外向的で感情豊かな炎タイプ        ┃  ← 軸スコアから生成した説明文
┃  ─────────────────────             ┃
┃  最近、周りを巻き込む行動が        ┃  ← personalityNarrative（週次。なければ非表示）
┃  目立っています                    ┃
└────────────────────────────────────┘
```

- カードタップ → 5軸スコア内訳モーダル

**情報収集中（signalCount < 30）:**

```
┌────────────────────────────────────┐
│  性格タイプを解析中                │
│                                    │
│  ████████░░░░░░░░  16/30           │  ← signalCount / 30
│                                    │
│  チャットや会議を続けると、あなた  │
│  の性格タイプが判定されます        │
└────────────────────────────────────┘
```

#### 「〇〇な炎タイプ」説明文の生成テンプレート

軸スコアから2〜3フレーズを選んで連結（AI不要・固定テンプレート）。

| 軸 | 条件 | フレーズ |
|----|------|---------|
| エネルギー | > 0.3 | 「外向的で」 |
| エネルギー | < -0.3 | 「内省的で」 |
| 判断基準 | < -0.3 | 「感情豊かな」 |
| 判断基準 | > 0.3 | 「論理的な」 |
| 関わり方 | > 0.3 | 「協調を大切にする」 |
| 関わり方 | < -0.3 | 「自分軸の強い」 |
| 行動スタイル | > 0.3 | 「計画的な」 |
| 行動スタイル | < -0.3 | 「自由奔放な」 |
| 処理スタイル | > 0.3 | 「分析的な」 |
| 処理スタイル | < -0.3 | 「直感的な」 |

#### 人格解析セクション（仕事・恋愛・ストレス・学習・意思決定）

- **表示トリガー**: `element` フィールドが存在する（性格タイプが確定した時）
- **生成タイミング**: signalCount 30到達時に `generateCharacterDetails` を呼び出し非同期生成
- **データなし時のUI**: 「生成中...」スピナー（`analysisData?.analysis100?[category] == null` で判定）

### ホーム画面

- Big5診断ボタン・100問プログレスバーは廃止
- 「性格診断して」「性格解析して」キーワード: クライアント側でインターセプトし、チャット自動解析の説明文を返す（Cloud Functionへの転送なし）
- 自分会議の解放条件: `signalCount >= 30`

#### 成長ゲージ（`_GrowthGauge`）

アクションボタン（自分会議・履歴）とチャット入力の間に常時表示されるカード型進捗バー。

| signalCount | バッジ | ラベル | カウント | プログレス |
|------------|--------|--------|---------|---------|
| 0〜29 | 赤ちゃん | 幼少期まで | X / 30 | signalCount / 30 |
| 30〜99 | 幼少期 | 大人まで | X / 100 | signalCount / 100 |
| 100以上 | 大人 | 性格解析 | (X%10) / 10 | (signalCount % 10) / 10 |

```
┌──────────────────────────────────────┐  ← 白背景カード（opacity 0.88）
│  [幼少期]  大人まで        42 / 100  │  ← バッジ(アクセントカラー) + ラベル + カウント
│  ██████████░░░░░░░░░░░░░░░           │  ← LinearProgressIndicator（アクセントカラー）
└──────────────────────────────────────┘
```

- バッジ: アクセントカラー地・白文字・角丸ピル
- 100+は10シグナルごとにゲージがリセット（解析サイクルの可視化）

#### 進化ダイアログ（`_TypeEvolutionDialog`）

性格タイプ（element / typeName）が変化した際に全画面オーバーレイで表示されるアニメーション演出。

**表示トリガー**:
- `personalityMeta/current.pendingTypeChangeNotification == true` かつ `newElement` / `newTypeName` フィールドが存在する
- `pendingTypeChangeProvider`（StreamProvider）を `ref.listen` で購読し、変化を検知したら表示

**実装ファイル**: `presentation/screens/home/home_screen.dart`（`_TypeEvolutionDialog` クラス）

**演出内容**:
| 要素 | 内容 |
|------|------|
| 背景 | 半透明黒（opacity 0.85）バリア |
| コンテナ | ダーク背景 + 元素カラーのボーダー・グロー（`BoxShadow`） |
| キャラクターアイコン | 元素の絵文字（🔥💨⚡✨💧🌿❄️🌑⭐）をClipOvalで丸表示 |
| グローアニメーション | `AnimationController`（1500ms, repeat+reverse）でBoxShadowをパルス |
| 入場アニメーション | `AnimationController`（800ms）でフェードイン + スケールアップ |
| テキスト | 元素名・属性表示 + `{typeName} になりました！` |
| シェアボタン | 「シェアする」→ 画面外静的カード（left: -9999）を `RepaintBoundary.toImage()` でキャプチャしてPNG画像としてOSシェアシートに渡す。キャプチャ失敗時はテキストのみにフォールバック。詳細は [`SNS共有仕様書.md`](SNS共有仕様書.md) 参照 |
| テキスト | `DARIASで「{typeName}」になりました！ #DARIAS #性格診断` |
| 確認ボタン | 「確認する」→ `pendingTypeChangeNotification: false` を Firestore に書き込んでダイアログを閉じる |

**元素カラー表**:
| 元素 | カラー |
|------|-------|
| 炎 | `#FF6B35` |
| 風 | `#64B5F6` |
| 雷 | `#FFD54F` |
| 光 | `#FFF9C4` |
| 水 | `#42A5F5` |
| 土 | `#8D6E63` |
| 氷 | `#80DEEA` |
| 闇 | `#CE93D8` |
| 無 | `#B0BEC5` |

**Firestoreフラグのライフサイクル**:
1. Cloud Function (`calculateAndSaveAxisScores`) が element/typeName の変化を検知 → `pendingTypeChangeNotification: true`、`newElement`・`newTypeName` をセット
2. Flutter がリアルタイムで検知 → ダイアログ表示
3. ユーザーが「確認する」ボタンをタップ → `pendingTypeChangeNotification: false` に更新してフラグクリア

**重複表示防止**: `_isShowingEvolutionDialog` フラグで同時に複数ダイアログが表示されないよう制御

**夢の提案セクション**: 性格タイプが変わって夢の候補が作り直された場合（`dream/current.pendingDreamProposal == true`）、ダイアログ内に現在の夢と「このまま」「候補を見る」を表示する。**現在の夢は自動で差し替えない。** 「確認する」で閉じたときもフラグを下ろす（このダイアログは性格変化時にしか出ないため、残すと再表示の機会がない）。

---

## 夢の選択フロー

キャラクターの夢は、性格から生成した5個の候補からユーザーが選ぶ。1個だけを提示すると「それはやりたくない」となった際に離脱の原因になるため。既に叶えたい夢がある人のために自由入力欄も用意する。

### データの持ち方

| 場所 | フィールド | 内容 |
|------|-----------|------|
| `CharacterDetailsTemplate/{personalityKey}` | `dreams` | AIが生成した候補5個（同じ性格の全ユーザーで共有） |
| `users/{uid}/characters/{cid}/dream/current` | `dreamOptions` | そのユーザーに提示中の候補5個 |
| 同上 | `dream` | 現在採用中の夢（1個） |
| 同上 | `dreamSource` | `user`（ユーザーが選択・入力）/ `ai`（暫定採用） |
| 同上 | `pendingDreamProposal` | 新しい候補ができたことを示すフラグ |

**`details/current` には夢を置かない。** `details` は firestore.rules で認証済みユーザーの単体取得が許可されており（フレンドのアバター表示用）、自由入力を含む夢が他ユーザーから読めてしまうため。

### 生成と上書きのルール

- 候補は `generateCharacterDetails` が `dreamOptions` に保存する（`src/utils/dreamStore.js` の `saveDreamOptions`）
- **`dream` は再生成で上書きしない。** ユーザーが選んだ夢が性格変化で勝手に消えないようにするため
- 既存ユーザーの `details/current.dream` は、初回の候補保存時に `dream/current` へ移行する（`dreamSource: 'ai'`）。読み取り側（`generateCharacterReply` / `generateDiary`）は未移行ユーザーのために `details.dream` へフォールバックする

### 選択の導線

| タイミング | 画面 | 挙動 |
|-----------|------|------|
| 初回（signalCount 30到達） | ホーム画面のダイアログ | `pendingFirstDreamSelection` を検知して「性格が決まりました」＋夢選択を表示。「あとで選ぶ」を押した場合は候補1個目を暫定採用（`dreamSource: 'ai'`）し、チャットが「夢はまだ決まっていません」に切り替わるのを防ぐ |
| 性格タイプ変化時 | 進化ダイアログ内 | `pendingDreamProposal` を検知して提案。現在の夢は維持 |
| 性格キー変化時・既存ユーザーへの配布時 | ホーム画面のポップアップ | `pendingDreamProposal` を検知して単独のダイアログを表示。進化ダイアログはタイプ名が変わったときしか開かないため、それ以外の経路を拾う |
| いつでも | キャラクター詳細画面の「夢」欄 | 「変更」ボタンから選び直せる |

**ポップアップと進化ダイアログは重ねない**: 進化ダイアログ・初回選択ダイアログの表示中はポップアップを抑制し、進化ダイアログを閉じるときに `pendingDreamProposal` が下りるため二重には出ない。

**選択シートから戻れる**: シート左上の戻るボタン（または下スワイプ）で閉じると、呼び出し元のポップアップへ戻り「このまま」を選び直せる。夢を保存したときだけ完了する。詳細画面から開いた場合は、閉じると何も起きない。

**シートの見た目**: 背景はホーム画面と同じ `backgroundGradientProvider` のグラデーションを使い、背景の明度から文字色（濃いグレー／白）を決める。タイトルは中央揃え。「自分で入力する」の初期値には現在の夢が入る（候補に含まれているかどうかに関わらず）。

### 入力のサニタイズ

夢はチャット（`characterReply`）と日記（`activityDiary`）のシステムプロンプトに `夢: ○○` の形で埋め込まれるため、プロンプトインジェクションの経路になりうる。クライアント（`DreamService.sanitize`）とCloud Functions（`sanitizeDream`）の両方で以下を適用する。

- 制御文字（改行・タブを含む）を空白に置換し、連続する空白を1つにまとめる
- **40文字**で切り詰める（サロゲートペアを割らないようコードポイント単位）
- 空文字は保存しない

### 実装ファイル

| 層 | ファイル |
|----|---------|
| Cloud Functions | `src/utils/dreamStore.js`（保存・取得・サニタイズ）、`const/generateCharacterDetails.js`（候補生成）、`src/personality/axisCalculator.js`（初回フラグ・キー変化での再生成）、`const/generatePersonalityKey.js`（キー生成・変化判定） |
| Flutter | `presentation/providers/dream_provider.dart`（状態・保存）、`presentation/widgets/dream_select_sheet.dart`（選択シート）、`screens/home/home_screen.dart`（初回ダイアログ・進化ダイアログ内の提案・新候補のポップアップ）、`screens/character/character_detail_screen.dart`（`_DreamRow`） |

---

## キャラクター成長システム

### 概要

`signalCount` に連動してキャラクターが3段階に成長する。成長段階ごとにローカルアセット画像を切り替え、元素確定（signalCount >= 30）後は属性別エフェクトを表示する。

### 成長段階と画像アセット

| ステージ | signalCount | ファイル | 備考 |
|---------|------------|---------|------|
| 赤ちゃん | 0〜29 | `assets/images/character_growth/赤ちゃん.png` | 全ユーザー共通 |
| 幼少期 | 30〜99 | `assets/images/character_growth/幼少期_{元素}.png` | 9元素 × 1枚 |
| 大人 | 100以上 | `assets/images/character_growth/成人_{性別}_{元素}.png` | 2性別 × 9元素 = 18枚 |

アセット合計: 1 + 9 + 18 = **28枚**

### パス生成関数（`characterGrowthAssetPath`）

`lib/presentation/widgets/character/element_effect_widget.dart` に定義。

```dart
String characterGrowthAssetPath({
  required int signalCount,
  required String? element,
  required String? gender,
}) {
  if (signalCount < 30 || element == null) return '赤ちゃん.png';
  if (signalCount < 100) return '幼少期_$element.png';
  final g = gender == '男性' ? '男性' : '女性';
  return '成人_${g}_$element.png';
}
```

### 元素エフェクト（`ElementEffectWidget`）

`signalCount >= 30`（元素確定後）に、キャラクター画像の背後に属性別アニメーションエフェクトを表示する。

- **表示画面**: ホーム画面・キャラクター詳細画面
- **非表示画面**: カレンダー画面（軽量化のため除外）

### キャラクターアバター（`CharacterAvatarWidget`）

設定画面アカウントカード・フレンド一覧・フレンド検索で使用する丸アバター。

- **表示方式**: `ClipOval` + `OverflowBox(maxHeight: size * 1.8, alignment: Alignment.topCenter)` で1.8倍ズームして上部（顔部分）をトリミング表示
- **自分のアバター**: `signalCountProvider` + `characterDetailsProvider` をリアルタイム購読
- **フレンドアバター**: `userCharacterDetailsProvider(userId)` から `growthStage` と `element` を取得し段階を決定
- **フォールバック**: `growthStage == 0` かつ `element != null` → 幼少期として扱う（growthStage 書き込み前の既存ユーザー向け）

### Flutter ファイル一覧（追加分）

| ファイル | 内容 |
|---------|------|
| `presentation/widgets/character/element_effect_widget.dart` | `elementTypeFromString()` / `characterGrowthAssetPath()` ユーティリティ関数を追加 |
| `presentation/widgets/character_avatar_widget.dart` | 成長段階画像 + 顔クロップ丸アバターに全面改修 |
| `presentation/screens/home/home_screen.dart` | 成長段階キャラクター表示（`_CharacterDisplay`）+ 成長ゲージ（`_GrowthGauge`）追加 |
| `presentation/screens/character/character_detail_screen.dart` | 成長段階キャラクター表示（`_CharacterImage`）+ 成長ゲージカード（`_GrowthGaugeCard`）追加 |
| `presentation/screens/calendar/calendar_screen.dart` | 成長段階キャラクター表示（エフェクトなし） |

---

## Firestoreスキーマ（新規追加分）

### `users/{userId}/personalitySignals/{signalId}`（新規）

1回のメッセージから抽出したタグを1ドキュメントとして保存する。

```json
{
  "tags": ["social_reference", "emotional_expression"],
  "timestamp": "Timestamp",
  "messageType": "chat"
}
```

### `users/{userId}/personalityMeta/current`（新規）

```json
{
  "signalCount": 42,
  "pendingTypeChangeNotification": false
}
```

- `signalCount`: 累積シグナル数。`savePersonalitySignal` がトランザクションでインクリメント
- Flutter `signalCountProvider` がこのドキュメントをリアルタイム購読して進捗バーに使用

### `users/{userId}/characters/{charId}/details/current`（既存ドキュメントへの追加フィールド）

```json
{
  "axisScores": {
    "energy":       0.4,
    "judgment":    -0.3,
    "relationship": 0.2,
    "lifestyle":    0.1,
    "processing":  -0.2
  },
  "element":    "炎",
  "typeName":   "場を沸かす炎タイプ",
  "convertedBig5Scores": {
    "openness": 2.8,
    "conscientiousness": 3.4,
    "extraversion": 3.8,
    "agreeableness": 3.6,
    "neuroticism": 2.1
  },
  "personalityNarrative": "最近、周りを巻き込む行動が目立っています",
  "axisUpdatedAt":      "Timestamp",
  "axisGeneratedAt":    "Timestamp",
  "narrativeUpdatedAt": "Timestamp"
}
```

- `axisGeneratedAt`: null なら signalCount 30到達時に `generateCharacterDetails` を実行・保存。初回生成を1回に限るためのフラグ
- キャラクター属性（`favorite_color` / `favorite_word` / `strength` など10項目）は `CharacterDetailsTemplate/{personalityKey}` からコピーされる。チャット・日記の参照先を変えないため、共有テンプレートを直接読ませずここに複製している
- `favorite_word`（口癖）は生成時に条件を課す: **12文字以内 / 独り言としても会話の中でも自然に使える言い切りの形 / 相手への相づち（`面白いね！`）や単語だけ（`安定`）は不可 / 性格に合ったトーン（外向的なら前向き、内省的なら落ち着いた言葉） / 迷い・不安の調子は神経症傾向が高いときだけ / 鉤括弧・引用符を含めない**。日記の ai_comment はキャラクターの独白なので、会話相手がいないと成立しない言葉だと文脈に合わなくなるため。保存時にも `pickAttributes()`（`generateCharacterDetails.js`）で外側の鉤括弧・引用符を取り除く
- **`dream` は含まない**（本人限定の `dream/current` に置く。既存ユーザーの残存値は移行後もフォールバック用に残る）
- 既存フィールド（`confirmedBig5Scores`・`analysisLevel` 等）はそのまま残存（既存ユーザーの後方互換）

### `users/{userId}/characters/{charId}/dream/current`（新規）

```json
{
  "dream":                 "世界中を旅して回ること",
  "dreamOptions":          ["夢1", "夢2", "夢3", "夢4", "夢5"],
  "dreamSource":           "user",
  "pendingDreamProposal":  false,
  "dreamOptionsUpdatedAt": "Timestamp",
  "dreamUpdatedAt":        "Timestamp",
  "updated_at":            "Timestamp"
}
```

`details` と違い全体公開の例外ルールが無いため、`users/{userId}/{subcollection=**}` の本人限定ルールが適用される。

### `CharacterDetailsTemplate/{personalityKey}`（新規・トップレベル）

```json
{
  "personality_key": "O3_C4_E2_A5_N1_female",
  "attributes":      {"favorite_color": "...", "favorite_word": "...", "...": "..."},
  "dreams":          ["夢1", "夢2", "夢3", "夢4", "夢5"],
  "model":           "gpt-4o-2024-11-20",
  "updated_at":      "Timestamp"
}
```

同じ personalityKey なら `characterDetails` プロンプトが文字列レベルで一致するため、生成結果を全ユーザーで共有する（`Big5Analysis` と同じ方針）。Cloud Function 専用で、firestore.rules ではクライアントからの読み書きを禁止している。

### `users/{userId}/characters/{charId}/big5Progress/current`（既存・後方互換）

旧BIG5 100問診断の進捗。新規ユーザーは使用しない。既存ユーザーのデータとして残存。

---

## `analysisLevel` フィールドの扱い

BIG5 100問診断廃止に伴い、`analysisLevel` フィールドは新システムでは使用しない。

| ユーザー種別 | 状態 |
|------------|------|
| 既存ユーザー | フィールドが存在するが参照しない（レガシーデータとして残す） |
| 新規ユーザー | BIG5質問がないためフィールドが生成されない（0のまま） |

Flutter の表示ロジックは `analysisLevel` への依存を除去し、代わりに `element` の有無で判定する。

---

## Cloud Functions ファイル一覧

| ファイル | 内容 |
|---------|------|
| `const/classifyAndExtract.js` | タグ抽出をGPTレスポンスに追加・`savePersonalitySignal` でFirestore保存 |
| `src/prompts/templates.js` | `classifyAndExtract` プロンプトにタグ抽出指示を追加。`buildPersonalityTraitsFromAxes()` を追加 |
| `src/personality/tagAxisMap.js` | タグと5軸のマッピング定数 |
| `src/personality/axisCalculator.js` | タグ→5軸スコア計算・元素判定・BIG5変換アルゴリズム。30シグナル到達時に `generateCharacterDetails` を呼び出す |
| `const/generatePersonalityNarrative.js` | 週次ナラティブ生成（Cloud Scheduler・毎週日曜2:00 JST） |
| `const/generateCharacterReply.js` | `axisScores` がある場合に `buildPersonalityTraitsFromAxes()` を優先使用 |

---

## Flutter ファイル一覧

| ファイル | 内容 |
|---------|------|
| `presentation/providers/character_provider.dart` | `CharacterDetails` に `axisScores`・`element`・`typeName`・`personalityNarrative`・`convertedBig5Scores` フィールド追加。`signalCountProvider`（personalityMeta監視）追加。画像変更条件を `signalCount < 30 && analysisLevel == 0` のとき無効化 |
| `presentation/screens/character/character_detail_screen.dart` | 性格タイプカード追加。人格解析表示トリガーを `element != null` に変更。isLoading修正（カテゴリ単位判定） |
| `presentation/screens/home/home_screen.dart` | BIG5診断ボタン・プログレスバー削除。「性格診断して」キーワードをクライアントインターセプトに変更。自分会議解放条件を `signalCount >= 30` に変更 |

---

## Firestoreインデックス

`details` コレクショングループの `element` フィールドに COLLECTION_GROUP スコープのインデックスを追加済み（`firestore.indexes.json`）。週次ナラティブ生成で使用。

---

## 未対応・将来対応

- [ ] FCM送信: `pendingTypeChangeNotification = true` 時の実際のプッシュ通知送信（フラグのクリアのみ実装済み）

> 相性診断の仕様は `相性診断仕様書.md` を参照
