# 相性診断機能 設計書

> フレンドとのカテゴリ別相性診断機能の完全設計ドキュメント

**最終更新日**: 2026-05-10（ミラー保存の記載追加；FriendAskScreen / FriendAskHistoryScreen の仕様追加；Webブロック動作の明記；コストと収益セクションを機能横断で全面更新；_ChatBubble → CompatibilityChatBubble 更新）
**バージョン**: 1.0（カテゴリ別診断・広告モデル）

---

## 目次

1. [機能概要](#機能概要)
2. [フロー設計](#フロー設計)
3. [スコア算出アルゴリズム](#スコア算出アルゴリズム)
4. [Cloud Function 設計](#cloud-function-設計)
5. [Firestore スキーマ](#firestore-スキーマ)
6. [Flutter UI 設計](#flutter-ui-設計)
7. [キャッシュ戦略](#キャッシュ戦略)
8. [コスト・収益](#コストと収益)
9. [v2 設計（5軸スコアベース）](#v2-設計5軸スコアベース) ※未実装・設計のみ

---

## 機能概要

フレンドと自分の BIG5 性格スコアを比較し、**4つのカテゴリ**（友情・恋愛・仕事・信頼）ごとに相性診断を提供する。

### カテゴリ一覧

| カテゴリキー | 表示名 | 診断観点 |
|------------|--------|---------|
| `friendship` | 友情 | 友人としての日常的な付き合い方・笑いのセンス・困った時に頼れるか |
| `romance` | 恋愛 | 感情的な共鳴・ドキドキ感と安心感のバランス・愛情表現のスタイル |
| `work` | 仕事 | 役割分担・アイデア出し・締め切り意識・お互いの強みの活かし方 |
| `trust` | 信頼 | 誠実さ・約束を守る姿勢・正直に話せるか・長期的な信頼関係 |

### 各診断の出力

- **スコア**: 0〜100% の数値（BIG5から決定論的に算出）
- **キャラクター会話**: 自分と相手のキャラクターが4〜5ターン語り合う会話（AI生成）
- **コメント**: 相性の現状を表す30文字以内の短文（AI生成）
- **アドバイス**: 相性を活かすための60文字以内の具体的提案（AI生成）

---

## フロー設計

### ユーザーフロー

```
フレンド詳細画面
    ↓
「相性診断」ボタン
    ↓
CompatibilityScreen（カテゴリ選択画面）
    ├── 既存スコア表示（全4カテゴリのスコアバー）
    │   ※ スコアは双方のBIG5から即時算出（API不要）
    ├── 各カテゴリカード
    │   ├── 未解放: 「リワード広告で診断」ボタン（Webでは「アプリ版で」）
    │   └── 解放済み: 診断結果サマリー → タップで詳細画面へ
    │       ↓（未解放の場合、アプリ版のみ）
    │   リワード広告を視聴
    │       ↓ 視聴完了
    │   Cloud Function `diagnoseCompatibility` を呼び出し
    │       ↓（約2〜5秒）
    │   CompatibilityCategoryScreen（カテゴリ詳細画面）
    │   └── アニメーション付きで会話 → スコア → コメント → アドバイスを表示
    └── 「○○のことを聞く」ボタン → FriendAskScreen へ
```

**Webでの動作:**
- 診断実行は **Web版では完全にブロック**（`kIsWeb` チェック、プレミアム問わず）
- フレンドが診断した結果（ミラー保存）は閲覧可能
- 未解放カードのボタンラベルは「アプリ版で」と表示

### スコア先行表示の仕組み

スコアは Cloud Function を呼び出す前にクライアント側で計算できる（決定論的アルゴリズム）。
ただし現在の実装では Cloud Function が返す `scores` マップをそのまま使用している。

将来の最適化: クライアント側でスコアのみ先行計算し、API呼び出し後に会話・コメントを追加表示する形にすることでUXを向上できる。

---

## スコア算出アルゴリズム

### 類似度関数

```
calcSimilarity(a, b) = max(0, 100 - |a - b| × 20)
```

BIG5スコアは1〜5の整数。差分0なら100%、差分1なら80%、差分5なら0%。

### カテゴリ別スコア計算式

#### 友情 (friendship)
```
friendship = round(
    calcSimilarity(my.E, friend.E) × 0.40
  + (my.A + friend.A) / 2 × 20 × 0.40
  + (100 - |my.N - friend.N| × 15) × 0.20
)
```
- **外向性の類似度** (40%): 社交スタイルが合うか
- **協調性の平均** (40%): お互いに気遣えるか
- **神経症傾向の差** (20%): 精神的な安定感の差

#### 恋愛 (romance)
```
romanceBase = round(
    calcSimilarity(my.N, friend.N) × 0.40
  + (my.A + friend.A) / 2 × 20 × 0.35
  + calcSimilarity(my.O, friend.O) × 0.25
)
romance = clamp(romanceBase, 20, 82)
```
- **神経症傾向の類似度** (40%): 感情の揺れが似ているか
- **協調性の平均** (35%): お互いを大切にする姿勢
- **開放性の類似度** (25%): 趣味や価値観の共有
- **上限82%/下限20%**: 恋愛相性に現実感を持たせるクランプ

#### 仕事 (work)
```
work = round(
    (my.C + friend.C) / 2 × 20 × 0.50
  + calcSimilarity(my.O, friend.O) × 0.50
)
```
- **誠実性の平均** (50%): 責任感・締め切り意識
- **開放性の類似度** (50%): アイデアの発想スタイル

#### 信頼 (trust)
```
trust = round(
    (my.A + friend.A) / 2 × 20 × 0.40
  + (100 - (my.N + friend.N) / 2 × 15) × 0.35
  + (my.C + friend.C) / 2 × 20 × 0.25
)
```
- **協調性の平均** (40%): 誠実さ・気遣い
- **神経症傾向の低さ** (35%): 安定した関係
- **誠実性の平均** (25%): 約束を守る姿勢

#### 総合 (overall)
```
overall = round((friendship + romance + work + trust) / 4)
```

全スコアは `clamp(0, 100)` で0〜100の範囲に収める（romance以外）。

### 変数の対応
| 変数 | BIG5特性 |
|------|---------|
| O | 開放性 (Openness) |
| C | 誠実性 (Conscientiousness) |
| E | 外向性 (Extraversion) |
| A | 協調性 (Agreeableness) |
| N | 神経症傾向 (Neuroticism) |

---

## Cloud Function 設計

### 関数仕様

| 項目 | 値 |
|-----|-----|
| 関数名 | `diagnoseCompatibility` |
| ソース | `const/diagnoseCompatibility.js` |
| API バージョン | v2 (`firebase-functions/v2/https`) |
| リージョン | `asia-northeast1` |
| メモリ | `512MiB` |
| タイムアウト | `90秒` |
| モデル | `gpt-4.1-mini` |
| temperature | `0.75` |
| max_tokens | 指定なし |

### 処理フロー

```
1. 入力検証
   - userId, friendId, category の存在確認
   - category が "friendship"/"romance"/"work"/"trust" のいずれかであることを確認

2. BIG5情報取得（並列）
   - fetchCharacterInfo(userId)
   - fetchCharacterInfo(friendId)
   → users/{uid}/characters/{charId}/details/current の confirmedBig5Scores を取得

3. スコア計算（同期・決定論的）
   - calcGenreScores(myInfo, friendInfo)
   - 全4カテゴリ + overall を算出

4. BIG5キー生成（キャッシュキー）
   - 各ユーザーの BIG5 指紋を生成: "o{O}c{C}e{E}a{A}n{N}"
   - 2つの指紋をアルファベット順でソートして "|" で連結
   - needsFlip = ソート後に順序が変わったかどうか（会話の isMyCharacter 反転に使用）

5. カテゴリ別キャッシュ確認
   - キャッシュキー: "{big5Key}_{category}"
   - Firestore: compatibilityCache/{cacheKey}
   ├── キャッシュヒット:
   │   - conversation の isMyCharacter を needsFlip で反転
   │   - saveToUserDoc() でユーザードキュメントに保存
   │   - 結果を返す
   └── キャッシュミス: → 6へ

6. AI 生成（キャッシュミス時）
   - buildSpeechStyle() で双方の口調・一人称をプロンプト化
   - JSON 形式で {conversation, comment, advice} を生成
   - フォールバック: GPT 失敗時はデフォルト会話を使用

7. キャッシュ保存
   - compatibilityCache/{cacheKey} に保存

8. ユーザードキュメント保存
   - users/{userId}/compatibilityResults/{friendId} に merge 保存
   - FieldValue.arrayUnion でカテゴリを unlockedCategories に追加

8.5. ミラー保存（フレンド側）
   - users/{friendId}/compatibilityResults/{userId} にも merge 保存
   - isMyCharacter を反転して保存（フレンドが閲覧する際に自分/相手が正しく表示されるよう）
   - フレンドが自分から相性診断を実行しなくても相手の診断結果を閲覧できる

9. 結果を返す
   - {comment, advice, conversation, big5Key, scores}
```

### プロンプト設計

#### キャラクター口調の生成 (`buildSpeechStyle`)

BIG5スコアと性別から自動的に口調・一人称を決定する。

```
性別: 女性 → 一人称「私」
      男性: E≥4 → 一人称「俺」 / E≤2 → 「僕」 / それ以外 → 「僕」

口調パターン（女性）:
  E≥4: 明るく社交的、「〜だよ」「〜ね」「〜かな」
  E≤2: おっとり、「〜かな」「〜ね」「〜だと思う」
  その他: 自然体、バランスよく

追加修飾:
  A≥4 → 「相手を気遣う優しい表現を好む」
  N≥4 → 「少し心配性で感情豊かな面がある」
  O≥4 → 「好奇心旺盛で新しいことに興味を示す」
  C≥4 → 「物事をしっかり考えてから話す」
```

#### システムプロンプト（概要）

- カテゴリ（友情/恋愛/仕事/信頼）特有のトピックに集中
- 会話4〜5ターン、交互
- キャラクター名は「自分」「相手」（固有名詞使わない）
- JSON形式: `{conversation: [{isMyCharacter, text}], comment, advice}`

---

## Firestore スキーマ

### `users/{userId}/compatibilityResults/{friendId}`

診断実行ユーザー視点の結果を保存する。

```json
{
  "scores": {
    "friendship": 78,
    "romance": 65,
    "work": 82,
    "trust": 71,
    "overall": 74
  },
  "unlockedCategories": ["friendship", "work"],
  "friendship": {
    "comment": "自然体で一緒にいられる良い関係",
    "advice": "定期的に気軽な会話を続けると絆が深まる",
    "conversation": [
      {"isMyCharacter": true, "text": "友達としての相性は78%だって！"},
      {"isMyCharacter": false, "text": "そうなんだ、なんか嬉しいね"},
      ...
    ],
    "big5Key": "o2c3e4a3n2|o3c4e3a4n2",
    "createdAt": Timestamp
  },
  "work": {
    "comment": "仕事でのチームワークは抜群",
    ...
  }
}
```

**書き込みルール:**
- `set({merge: true})` で追記的に保存
- 新しいカテゴリを診断するたびにそのカテゴリフィールドが追加される
- `scores` は最新の診断で上書き（BIG5スコア変化時も対応）
- `unlockedCategories` は `FieldValue.arrayUnion` で追記（削除なし）

### `compatibilityCache/{cacheKey}`

AIが生成したカテゴリ別会話のキャッシュ。

```
ドキュメントID例: "o2c3e4a3n2|o3c4e3a4n2_friendship"
```

```json
{
  "comment": "自然体で一緒にいられる良い関係",
  "advice": "定期的に気軽な会話を続けると絆が深まる",
  "conversation": [
    {"isMyCharacter": true, "text": "友達としての相性は78%だって！"},
    {"isMyCharacter": false, "text": "そうなんだ、なんか嬉しいね"},
    ...
  ],
  "big5Key": "o2c3e4a3n2|o3c4e3a4n2",
  "createdAt": Timestamp
}
```

**キャッシュの対称性:**
- BIG5指紋はソートされているため、A↔B と B↔A は同じキャッシュドキュメントを参照する
- 再利用時は `needsFlip` フラグで `isMyCharacter` を反転することで、視点を正しく切り替える

---

## Flutter UI 設計

### 画面構成

```
CompatibilityScreen（カテゴリ選択・概要画面）
├── CompatibilityCategoryScreen（カテゴリ詳細・会話画面）
└── FriendAskScreen（フレンドのことを聞く）
    └── FriendAskHistoryScreen（質問履歴一覧）
```

#### FriendAskScreen

**ソース**: `lib/presentation/screens/friend/friend_ask_screen.dart`

**機能:**
- テーマ別チップ（誕生日プレゼント等5種）タップで入力欄に自動挿入
- 質問を入力（最大100文字）して「聞く」ボタン押下
- 双方のキャラクターが会話（1400ms/ターン）してフレンドの好みや傾向を語り合う
- 会話終了後に lightbulb アイコンでおすすめを表示
- 免責事項を表示（「キャラクターの性格データをもとにした参考情報」）
- AppBar に履歴ボタン（履歴画面へ遷移）

**利用制限（無料ユーザー）:**
- `AskFriendLimitManager`（SharedPreferences）が1日1回の無料利用を管理
- 超過後は「動画を見てもう1回聞こう」ダイアログ → リワード広告視聴で1回追加
- Webでは制限なし（広告非対応のため）

**有料ユーザー:** 制限なし

#### FriendAskHistoryScreen

**ソース**: `lib/presentation/screens/friend/friend_ask_history_screen.dart`

**機能:**
- `askHistoryProvider` から全履歴を取得し、該当フレンドの履歴のみ表示
- カード形式で質問・日時・おすすめを常時表示
- カードタップで会話詳細を展開表示（アコーディオン）
- 最新50件まで表示

### CompatibilityScreen

**ソース**: `lib/presentation/screens/friend/compatibility_screen.dart`

**表示内容:**
- 全4カテゴリのスコアバー（スコアはCloud Functionレスポンスまたはローカル計算）
- 各カテゴリカード（未解放/解放済み）
- 未解放カード → 広告視聴ボタン

**カテゴリメタ情報 (`CompatibilityCategoryMeta`):**
```dart
class CompatibilityCategoryMeta {
  final String key;     // "friendship", "romance", "work", "trust"
  final String label;   // "友情", "恋愛", "仕事", "信頼"
  final String icon;    // 絵文字アイコン
  final Color color;    // カテゴリカラー
}
```

| key | label | icon | color |
|-----|-------|------|-------|
| friendship | 友情 | 🤝 | Colors.green |
| romance | 恋愛 | 💕 | Colors.pink |
| work | 仕事 | 💼 | Colors.blue |
| trust | 信頼 | 🌟 | Colors.orange |

**診断実行フロー（Flutter側）:**

```dart
// 1. リワード広告を視聴
await RewardedAdManager.show(onRewarded: () async {
  // 2. Cloud Function 呼び出し
  final diagnosis = await ref.read(friendControllerProvider.notifier)
      .runCategoryDiagnosis(friendId: friend.id, category: cat.key);
  // 3. 詳細画面に遷移（animateOnEntry: true）
  Navigator.push(context, CompatibilityCategoryScreen(
    friend: friend,
    category: cat,
    diagnosis: diagnosis!,
    animateOnEntry: true,
  ));
});
```

### CompatibilityCategoryScreen

**ソース**: `lib/presentation/screens/friend/compatibility_category_screen.dart`

**表示内容（animateOnEntry: true の場合）:**

1. ローディングインジケーター（400ms 遅延後アニメーション開始）
2. キャラクター会話（1400ms 間隔で1ターンずつ表示）
3. 全ターン表示後600ms遅延してスコアバッジ表示
4. コメント＋アドバイス表示

**静的表示（animateOnEntry: false）:**
- 全会話・スコア・コメント・アドバイスを一括表示（再訪問時）

**SNS共有ボタン:**
- AppBar 右上にシェアアイコン（`Icons.share`）を表示（`_showResult == true` になってから出現）
- `Share.share()` でテキストを共有。iOS実機対応のため `GlobalKey` で座標を取得し `sharePositionOrigin` に渡す
- 共有テキスト形式:
  ```
  {cat.icon} {friendName}との{cat.label}の相性
  
  相性スコア: N%
  
  {comment}
  
  💡 {advice}（ある場合のみ）
  
  #DARIAS #相性診断
  ```

**チャット吹き出し (`CompatibilityChatBubble`):**
- 左側: 自分のキャラクター（アバター + 名前ラベル）
- 右側: フレンドのキャラクター（アバター + 名前ラベル）
- アバターサイズ: 32px
- 名前ラベル: 10px bold
- `compatibility_category_screen.dart` で定義。`friend_ask_screen.dart` と `friend_ask_history_screen.dart` からも `show CompatibilityChatBubble` でインポートして再利用

**スコアバー (`_AnimatedScoreBar`):**
- 1秒かけてアニメーション表示（Curves.easeOut）
- 200ms 遅延後に開始

### Firebase Functions レスポンスの型変換

Firebase Functions は `Map<Object?, Object?>` を返すが Dart は `Map<String, dynamic>` を期待するため、JSON round-trip で変換する必要がある。

```dart
final data = jsonDecode(jsonEncode(result.data)) as Map<String, dynamic>;
return CategoryDiagnosis.fromFunctionResult(data, category);
```

---

## キャッシュ戦略

### キャッシュの目的

1. **コスト削減**: 同じBIG5組み合わせへの重複API呼び出しを防ぐ
2. **レスポンス向上**: キャッシュヒット時は Firestore 読み取りのみ（< 1秒）

### キャッシュキーの設計

```
big5Key = sort([fingerprint(userA), fingerprint(userB)]).join("|")
cacheKey = big5Key + "_" + category
```

```
fingerprint(user) = "o{O}c{C}e{E}a{A}n{N}"
例: "o3c4e2a5n1"
```

キーをソートすることで `A診断B` と `B診断A` が同じキャッシュを参照する。

### isMyCharacter の反転

キャッシュは「最初に診断したユーザーの視点」で保存されている。後から参照する場合（自分が B の場合）、`isMyCharacter` を反転する必要がある。

```javascript
const needsFlip = sorted[0] !== myFp; // myFpがソート後に後ろになった場合
const conversation = cached.conversation.map(msg => ({
  ...msg,
  isMyCharacter: needsFlip ? !msg.isMyCharacter : msg.isMyCharacter,
}));
```

### キャッシュヒット率の考察

BIG5は各特性1〜5の整数なので1ユーザーのフィンガープリントは `5^5 = 3,125` 通り。
2ユーザーのペアは最大 `3,125^2 / 2 ≒ 488万` 通り（対称性考慮）× 4カテゴリ。

6人会議と異なり同じ性格タイプ同士のペアは少なく、キャッシュヒット率は低い（初期: < 5%）。
ただしコストが安いためヒット率が低くても問題ない。

---

## コストと収益

> **コスト詳細・月間試算の正規ドキュメントは `shared/docs/architecture/課金サービス仕様書.md` を参照すること。**  
> ここには本機能固有のトークン内訳のみ記載する。  
> モデル単価参考値（最終確認: 2026-05-10）。1 USD = 150 JPY で換算。

### 相性診断（diagnoseCompatibility・1カテゴリあたり）

```
[system] 会話生成ルール + JSONスキーマ（config値を含む） .. ~290 tokens
[user]   自分キャラの口調情報（buildSpeechStyle最大値） .. ~105 tokens
[user]   相手キャラの口調情報（buildSpeechStyle最大値） .. ~105 tokens
[user]   カテゴリスコア + 診断指示 ..................... ~60 tokens
---
入力合計（推定最大値）: ~560 tokens
出力（推定値）: ~200 tokens（JSON: 4〜5ターン会話 + comment 30文字 + advice 60文字）
※max_tokensの設定なし（gpt-4.1-mini固定、diagnoseCompatibility.js）
```

| 項目 | キャッシュミス | キャッシュヒット |
|-----|-------------|---------------|
| OpenAI API (gpt-4.1-mini) | 約¥0.08 | ¥0 |
| **合計** | **約¥0.08** | **≈ ¥0** |

### フレンドのことを聞く（askAboutFriend・1回あたり）

```
[system] 会話生成ルール + JSONスキーマ ................. ~280 tokens
[user]   自分キャラの口調情報（buildSpeechStyle最大値） .. ~105 tokens
[user]   相手キャラの口調情報（buildSpeechStyle最大値） .. ~105 tokens
[user]   質問テーマ（max100字）+ 指示文 ............... ~60 tokens
---
入力合計（推定最大値）: ~550 tokens
出力（推定値）: ~200 tokens（JSON: 4〜5ターン会話 + recommendation 60文字）
※max_tokensの設定なし（gpt-4.1-mini固定、askAboutFriend.js）
```

| 項目 | コスト |
|-----|------|
| OpenAI API (gpt-4.1-mini) | 約¥0.08 |

**利用制限と広告収益:**
- 無料ユーザー（iOS）: 1日1回無料、超過後はリワード広告視聴で+1回
- 無料ユーザー（Web）: 利用不可（広告非対応）→ プレミアム誘導ダイアログ表示
- 広告1回の期待収益: 約¥0.055（日本・eCPM ¥55想定）
- 1回実行コスト（¥0.08）> 広告1回収益（¥0.055）→ 広告単体では赤字、サブスク収益で補完
- 有料ユーザー: 無制限（約¥0.08/回）

### 広告収益との比較（相性診断）

| | 収益 | コスト | 利益 |
|--|-----|-------|-----|
| 1カテゴリ診断 | ¥1.00（リワード広告） | ¥0.08 | **¥0.92** |
| 4カテゴリ全診断 | ¥4.00 | ¥0.32 | **¥3.68** |

---

---

## v2 設計（5軸スコアベース）

> ステータス: 設計確定（未実装）
> 性格診断リニューアル（`性格解析仕様書.md`）と連動した次世代相性診断。
> 入力がBIG5スコアから5軸スコア（-1.0〜+1.0）に変わる。カテゴリ・UI・広告モデルは継続。

---

### 基本方針

| 項目 | v1（現行） | v2（新設計） |
|-----|---------|-----------|
| 入力 | BIG5スコア（1〜5の整数） | 5軸スコア（-1.0〜+1.0の小数） |
| スコア算出 | 各軸の差 × 固定係数 | 類似度 or 対比度 × カテゴリ別重み |
| AI入力 | BIG5数値 + 口調ルール | 軸スコア → 自然言語変換 |
| カテゴリ | 友情・恋愛・仕事・信頼（継続） | 同左 |
| 広告モデル | 継続 | 継続 |

---

### スコア計算の仕組み

軸スコアは -1.0〜+1.0 の小数。2種類の計算式を使い分ける。

**類似度（似ているほど高い・0〜100）**
```
similarity(a, b) = 100 - |a - b| × 50
※ 最大差は 2.0（例: +1.0 と -1.0）→ × 50 で 0〜100 に収まる
```

**対比度（逆のほど高い・0〜100）**
```
contrast(a, b) = |a - b| × 50
```

カテゴリごとにどちらを使うかが変わる。恋愛のエネルギー軸だけ対比度を使う（逆のほど惹かれ合う）。

---

### カテゴリ別の軸の重み付け

| 軸 | 友情 | 恋愛 | 仕事 | 信頼 |
|----|------|------|------|------|
| エネルギー | 類似度 40% | **対比度** 40% | 類似度 20% | 類似度 20% |
| 判断基準 | 類似度 20% | 類似度 35% | 類似度 30% | 類似度 40% |
| 関わり方 | 類似度 30% | 類似度 25% | 類似度 20% | 類似度 25% |
| 行動スタイル | 類似度 10% | ― | 類似度 30% | 類似度 15% |
| 処理スタイル | ― | ― | 類似度 20% | ― |

**計算式（共通パターン）**
```
score(category) = Σ ( calc(axis_a, axis_b) × weight )
  calc = similarity または contrast（軸・カテゴリごとに決まる）

最終スコア = clamp(round(score), 0, 100)
```

---

### 元素ペアごとの相性傾向

元素は軸スコアから導出されるため、軸スコアで計算すれば元素の関係性は自動的に反映される。元素ごとの代表的な軸スコア値（参考）:

| 元素 | エネルギー | 判断基準 | 処理スタイル |
|------|----------|---------|-----------|
| 炎 | +0.7 | -0.7 | ― |
| 水 | -0.7 | -0.7 | ― |
| 雷 | +0.7 | +0.6 | -0.5 |
| 光 | +0.7 | +0.6 | +0.5 |
| 氷 | -0.7 | +0.6 | -0.5 |
| 闇 | -0.7 | +0.6 | +0.5 |
| 風 | +0.5 | 0.0 | ― |
| 土 | -0.5 | 0.0 | ― |
| 無 | 0.0 | ― | ― |

**ペアの種類と傾向（恋愛カテゴリで比較）**

| ペアの種類 | 例 | 特徴 |
|-----------|---|------|
| 同じ元素 | 炎×炎 | 全軸一致 → 友情・仕事・信頼が高い。恋愛はエネルギー対比度が0で中程度 |
| 対の元素 | 炎×水 | エネルギー対比度大 + 判断基準類似度大 → 恋愛スコアが際立って高い |
| 近い元素 | 炎×雷 | エネルギー一致だが判断基準がずれる → 全体的に中程度 |
| 遠い元素 | 炎×氷 | エネルギー対比大だが判断基準も大きくずれる → 全体的に低め |

---

### AIへの自然言語変換

v2では元素名・タイプ名だけではAIに性格が正確に伝わらないため、軸スコアを自然言語に変換してプロンプトに渡す。

```
【入力】
axes: { energy: +0.7, judgment: -0.6, relationship: +0.4,
        lifestyle: -0.3, processing: -0.5 }
element: "炎"
typeName: "場を沸かす炎タイプ"

【出力（自然言語）】
「外向的で感情を大切にする性格。直感を信じて行動し、
周りを巻き込む力がある。協調的な面もあり、場の空気
を作るのが得意。」
```

この自然言語テキストを診断プロンプトに渡し、既存と同じパイプライン（会話・コメント・アドバイス生成）を利用する。

---

### キャッシュキーの変更

v1では BIG5 フィンガープリント（`o{O}c{C}e{E}a{A}n{N}`）をキャッシュキーに使用していたが、v2では5軸スコアを使用する。スコアは小数のため、小数第1位に丸めて文字列化する。

```
fingerprint_v2(user) = "e{E}j{J}r{R}l{L}p{P}"
  E = round(energy × 10) / 10
  J = round(judgment × 10) / 10
  ...

cacheKey_v2 = sort([fp_a, fp_b]).join("|") + "_" + category + "_v2"
```

v2キャッシュは v1キャッシュと別のドキュメントIDになるため、並行運用可能。

---

---

### ヘルプ画面の相性表

#### 表示内容

ヘルプ画面（`help_guide_screen.dart`）に「元素相性図」セクションを追加する。

**使用画像:** `assets/images/element_chart.png`
（元画像: `Desktop/画像・動画/アプリ/DARIAS/ロゴ/相性表.png`）

画像は以下を表す：
- 8元素が円状に配置、中央に無
- 対ペア（炎↔水、雷↔氷、光↔闇、風↔土）を双方向矢印で表示
- 無から全元素への点線（全元素と相性）

#### 画面上の配置

```
ヘルプ画面
└── 「元素と相性について」セクション
    ├── 相性図画像（全幅表示）
    ├── 読み方の説明テキスト
    │   「双方向矢印の元素同士は特に相性が良い対ペアです。
    │    無はすべての元素と一定の相性があります。」
    └── 対ペア説明リスト（4ペア）
        ├── 炎↔水: 「感情で深く繋がる。時にぶつかるほどの熱量」
        ├── 雷↔氷: 「同じ直感型。熱量の差が惹かれ合いを生む」
        ├── 光↔闇: 「同じ分析型。外向きと内向きで視点が逆」
        └── 風↔土: 「完全対極。自由と安定が刺激し合う」
```

#### 自分の元素との連動（任意拡張）

自分の元素が判明している場合、相性図内の自分の元素を強調表示（枠やグロー）する。対ペアの元素には「相性の良いタイプ」ラベルを付ける。

---

### 未決定事項（v2）

- [ ] 実装設計（Cloud Function の入力形式変更）
- [ ] v1 → v2 移行タイミング（性格診断リニューアル実装後）
- [ ] v1 キャッシュドキュメントの廃止方針

---

---

## 実装状況（2026-05-10 更新）

### v1（BIG5ベース）

| ファイル | 変更 |
|--------|------|
| `shared/functions/const/diagnoseCompatibility.js` | `confirmedBig5Scores` が null の場合に `convertedBig5Scores` へフォールバック追加 |
| `flutter/lib/presentation/screens/friend/compatibility_screen.dart` | `big5Done` チェックを `confirmedBig5Scores \|\| convertedBig5Scores` に変更。テキストを「性格解析」表記に更新 |
| `flutter/lib/presentation/providers/friend_provider.dart` | Cloud Function がエラーオブジェクトを返した場合に null を返すよう修正 |
| `flutter/lib/presentation/screens/settings/help_guide_screen.dart` | 「性格診断（BIG5）」→「性格解析」に更新。相性診断の説明も更新。「元素と相性について」セクション追加（相性表画像 + 対ペア説明） |
| `flutter/assets/images/element_chart.png` | `Desktop/画像・動画/アプリ/DARIAS/ロゴ/相性表.png` からコピーして追加 |
| `flutter/lib/presentation/screens/friend/friend_screen.dart` | ヘッダーに「?」ボタン追加。タップで `_ElementChartSheet`（元素相性表ボトムシート）を表示 |
| `flutter/lib/presentation/screens/onboarding/onboarding_screen.dart` | BIG5文言を削除。「まず性格診断から（BIG5）」→「性格解析について（チャット自動解析）」に変更 |

### フレンド画面「?」ボタン（元素相性表）

- 実装済み（2026-05-10）
- フレンド一覧ヘッダー右側に `?` アイコンボタンを追加
- タップで `_ElementChartSheet` ボトムシートを表示（相性表画像 + 対ペア4組の説明）

### v2（5軸スコアベース）

未実装。設計は本ドキュメント内の v2 セクション参照。

*最終更新: 2026-04-17（初版作成）*
*v2 設計追記: 2026-05-09*
*v1 動作修正: 2026-05-10（convertedBig5Scoresフォールバック・Flutter対応）*
*ヘルプ画面element_chart追加: 2026-05-10*
