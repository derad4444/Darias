# オンボーディング・ヘルプガイド仕様書

**作成日**: 2026-04-19
**対象プラットフォーム**: Flutter（iOS / Web）

---

## 目次

1. [概要](#概要)
2. [オンボーディングスライド](#オンボーディングスライド)
3. [使い方ガイド画面](#使い方ガイド画面)
4. [インラインヒントバナー](#インラインヒントバナー)
5. [相性診断 BIG5チェック](#相性診断-BIG5チェック)
6. [ファイル構成](#ファイル構成)

---

## 概要

新規ユーザーへの機能説明と、既存ユーザーへの操作ヒント表示を担う機能群。

| 機能 | 表示タイミング | 管理場所 |
|------|--------------|---------|
| オンボーディングスライド | 新規登録後・未視聴アカウントのログイン時 | Firestore `hasSeenOnboardingSlides` |
| 使い方ガイド画面 | 設定画面から任意で参照 | ハードコード |
| インラインヒントバナー | 各画面への初回アクセス時のみ | SharedPreferences（端末ローカル） |
| 相性診断BIG5チェック | 相性診断画面（常時） | Firestoreリアルタイム |

---

## オンボーディングスライド

### 表示フロー

```
新規登録
    ↓
register_screen.dart → context.go('/onboarding')

ログイン（splash経由）
    ↓
Firestore users/{uid}.hasSeenOnboardingSlides を確認
    ├── false または フィールドなし → /onboarding へ遷移
    └── true → / (ホーム) へ遷移
```

### スライド構成（4枚）

| # | タイトル | アイコン | 内容 |
|---|---------|---------|------|
| 1 | DARIASへようこそ | `auto_awesome` | アプリ概要 |
| 2 | チャットで何でもできる | `chat_bubble_outline` | チャット操作・Q&A・使い方ガイドの案内 |
| 3 | 3つのAI機能 | `psychology_outlined` | 日記・自分会議・相性診断の説明 |
| 4 | まず性格診断から | `star_outline` | BIG5診断の説明・使い方ガイドへの誘導 |

### 操作

| ボタン | 動作 |
|--------|------|
| スキップ | Firestoreに`hasSeenOnboardingSlides: true`を書き込み → ホームへ |
| 次へ | 次のスライドへ |
| 始める！（最終ページ） | Firestoreに`hasSeenOnboardingSlides: true`を書き込み → ホームへ |

### Firestore管理

- **フィールド**: `users/{userId}.hasSeenOnboardingSlides` (`boolean`)
- **設定タイミング**: スキップ or 始める！ボタン押下時
- **初期値**: フィールドなし（= `false` 扱い）
- **目的**: 端末・ブラウザ問わずアカウント単位での表示制御

> **注意**: 既存フィールド `hasCompletedOnboarding` はキャラクター作成完了フラグとして独立して使用。オンボーディングスライドの表示制御には `hasSeenOnboardingSlides` を使用する。

### 実装ファイル

- `lib/presentation/screens/onboarding/onboarding_screen.dart`
- `lib/presentation/screens/splash/splash_screen.dart`（ログイン時の判定）
- `lib/presentation/screens/auth/register_screen.dart`（登録後の遷移先）
- `lib/presentation/router/app_router.dart`（`/onboarding` ルート）

---

## 使い方ガイド画面

### アクセス方法

設定画面 → 「使い方ガイド」

### 画面仕様

- 上部に**検索バー**（セクションタイトル・本文の両方を対象にフィルタリング）
- 該当なし時は「該当する機能が見つかりませんでした」を表示
- 各セクションは `ExpansionTile` で展開・折りたたみ
- 背景色はアプリのテーマカラー（`backgroundGradientProvider`）に準拠

### セクション一覧（11セクション）

| # | タイトル | アイコン |
|---|---------|---------|
| 1 | チャット | `chat_bubble_outline` |
| 2 | 予定（カレンダー） | `calendar_today_outlined` |
| 3 | メモ | `note_outlined` |
| 4 | タスク（TODO） | `check_box_outlined` |
| 5 | 日記（自動生成） | `auto_stories_outlined` |
| 6 | 性格診断（BIG5） | `psychology_outlined` |
| 7 | 自分会議（6人会議） | `groups_outlined` |
| 8 | フレンド | `people_outline` |
| 9 | 相性診断 | `favorite_border` |
| 10 | プレミアム機能 | `star_outline` |
| 11 | 設定・カスタマイズ | `settings_outlined` |

### コンテンツ管理

現状はコード内にハードコード（`_kSections` const配列）。  
将来的に画像付きコンテンツが必要になった場合はFirestore管理に移行することを検討。

### 実装ファイル

- `lib/presentation/screens/settings/help_guide_screen.dart`
- `lib/presentation/router/app_router.dart`（`/help-guide` ルート）

---

## インラインヒントバナー

### 概要

各画面への**初回アクセス時のみ**表示される説明バナー。  
アニメーション（`SizeTransition`）付きで表示され、×ボタンで閉じると以降は表示されない。

### 管理方法（SharedPreferences）

キー形式: `hint_v1_{feature}_{userId}`

アカウント切り替えには対応しているが、**端末・ブラウザをまたいだ同期はしない**（ローカル保存）。  
→ 別端末では同じアカウントでも再表示される可能性あり（軽微な仕様として許容）

### 定義済み feature キー（`HintService`）

| 定数 | キー | 設置画面 |
|------|-----|---------|
| `kHome` | `home` | ホーム画面（3ステップバナー） |
| `kMemo` | `memo` | メモ編集画面 |
| `kMeeting` | `meeting` | 自分会議画面（初回に参加者説明ダイアログを自動表示） |
| `kFriend` | `friend` | フレンド一覧画面 |
| `kCompatibility` | `compatibility` | 相性診断画面 |
| `kCalendarDiary` | `calendar_diary` | カレンダー予定一覧（日記アイコン説明） |

### バナー種別

#### `InlineHintBanner`（汎用）

- 1メッセージを表示
- 右端の×ボタンで閉じる → `HintService.markShown()` を呼び出し

```dart
InlineHintBanner(
  userId: userId,
  feature: HintService.kMemo,
  message: 'ツールバーの「タスク」ボタンを押すと...',
  icon: Icons.check_box_outlined,
)
```

#### `HomeHintBanner`（ホーム専用）

- 3ステップをタップで順送り（「1/3 ›」カウンター表示）
- 全ステップ完了でバナーが閉じる
- ステップ進捗は `HintService.getHomeStep()` / `setHomeStep()` で管理

| Step | アイコン | 内容 |
|------|---------|------|
| 1 | `chat_bubble_outline` | チャットで予定・メモ・タスク登録 |
| 2 | `help_outline` | チャットQ&A + 設定から使い方ガイド |
| 3 | `psychology_outlined` | 性格診断の案内 |

### 設置一覧

| 画面 | ウィジェット種別 | 設置箇所 | 内容 |
|------|--------------|---------|------|
| ホーム画面 | `HomeHintBanner` | Stack上部（Positioned top:0） | 3ステップ機能紹介 |
| メモ編集 | `InlineHintBanner` | エディタの上 | タスクボタンの説明 |
| 自分会議 | ダイアログ自動表示 | initState（初回のみ） | 参加キャラクター説明 |
| フレンド一覧 | `InlineHintBanner` | フレンド一覧の上 | 共有レベルの説明 |
| 相性診断 | `InlineHintBanner` | アバター行の下 | 広告・BIG5必要条件の説明 |
| カレンダー予定一覧 | `InlineHintBanner` | 日記アイコンの上 | 日記アイコンの説明 |

### アカウント削除時のクリーンアップ

`HintService.clearAllForUser(userId)` を呼び出し、  
`_{userId}` で終わるすべてのSharedPreferencesキーを削除。  
→ `settings_screen.dart` の `_confirmDeleteAccount` 内で実行。

### 実装ファイル

- `lib/data/services/hint_service.dart`
- `lib/presentation/widgets/inline_hint_banner.dart`

---

## 相性診断 BIG5チェック

### 概要

相性診断は自分とフレンド双方のBIG5診断（100問完了）が必要。  
未完了の場合は「診断する」ボタンを非活性にし、理由を画面に表示する。

### 判定ロジック

```dart
// 自分のBIG5
final myDetails = ref.watch(characterDetailsProvider).valueOrNull;
final myBig5Done = myDetails?.confirmedBig5Scores?.isNotEmpty ?? false;

// フレンドのBIG5（userCharacterDetailsProvider で Firestore から取得）
final friendDetailsAsync = ref.watch(userCharacterDetailsProvider(friend.id));
final friendBig5Done = friendDetailsAsync.valueOrNull?.confirmedBig5Scores?.isNotEmpty ?? false;

// ロード中は誤表示を防ぐため活性にしておく
final big5DataLoaded = !characterDetailsProvider.isLoading && !friendDetailsAsync.isLoading;
final big5Ready = !big5DataLoaded || (myBig5Done && friendBig5Done);
```

### UI表示

| 状態 | 表示内容 |
|------|---------|
| 両方未完了 | 「診断には自分と〇〇さん双方のBIG5診断完了が必要です」 |
| 自分のみ未完了 | 「診断にはあなたのBIG5診断完了が必要です。チャットで"性格診断して"と送ってください」 |
| フレンドのみ未完了 | 「診断には〇〇さんのBIG5診断完了が必要です」 |
| 両方完了 | 警告非表示。「診断する」ボタン活性 |
| ロード中 | 警告非表示。「診断する」ボタン活性（誤表示防止） |

### 実装ファイル

- `lib/presentation/screens/friend/compatibility_screen.dart`
- `lib/presentation/providers/character_provider.dart`（`userCharacterDetailsProvider`）

---

## ファイル構成

```
lib/
├── data/
│   └── services/
│       └── hint_service.dart              # SharedPreferences ヒント管理
├── presentation/
│   ├── screens/
│   │   ├── onboarding/
│   │   │   └── onboarding_screen.dart     # オンボーディングスライド
│   │   ├── settings/
│   │   │   └── help_guide_screen.dart     # 使い方ガイド
│   │   ├── splash/
│   │   │   └── splash_screen.dart         # ログイン時オンボーディング判定
│   │   ├── auth/
│   │   │   └── register_screen.dart       # 登録後 /onboarding へ遷移
│   │   ├── home/
│   │   │   └── home_screen.dart           # HomeHintBanner 設置
│   │   ├── memo/
│   │   │   └── memo_detail_screen.dart    # InlineHintBanner（タスクボタン）
│   │   ├── meeting/
│   │   │   └── meeting_screen.dart        # 初回参加者説明ダイアログ
│   │   ├── friend/
│   │   │   ├── friend_screen.dart         # InlineHintBanner（共有レベル）
│   │   │   └── compatibility_screen.dart  # BIG5チェック + InlineHintBanner
│   │   └── calendar/
│   │       └── calendar_screen.dart       # InlineHintBanner（日記アイコン）
│   ├── router/
│   │   └── app_router.dart                # /onboarding, /help-guide ルート
│   └── widgets/
│       └── inline_hint_banner.dart        # InlineHintBanner / HomeHintBanner
```
