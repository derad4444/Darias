# 検索結果には出さないが、クロール自体は許可する。
#
# Disallow にすると、検索エンジンがページを取得できず
# <meta name="robots" content="noindex"> を読めないため、
# かえって索引に残ることがある（除外はメタタグ側で行う）。
#
# SNSがリンクを検証する際もページの取得が必要なため、
# Disallow にしているとリンクの追加に失敗することがある。
User-agent: *
Allow: /
